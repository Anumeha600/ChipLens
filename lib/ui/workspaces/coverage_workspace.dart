import 'package:flutter/material.dart';

/// Placeholder for the Coverage workspace.
///
/// Replace with coverage analysis UI in a future sprint.
class CoverageWorkspace extends StatelessWidget {
  const CoverageWorkspace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Coverage Workspace'));
  }
}
