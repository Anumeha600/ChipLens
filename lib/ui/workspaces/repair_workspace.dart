import 'package:flutter/material.dart';

/// Placeholder for the Repair Planning workspace.
///
/// Replace with repair planning UI in a future sprint.
class RepairWorkspace extends StatelessWidget {
  const RepairWorkspace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Repair Workspace'));
  }
}
