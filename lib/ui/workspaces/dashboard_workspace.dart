import 'package:flutter/material.dart';

/// Placeholder for the Dashboard workspace.
///
/// Replace the [Center] body with the real Dashboard content in a future sprint.
class DashboardWorkspace extends StatelessWidget {
  const DashboardWorkspace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Dashboard Workspace'));
  }
}
