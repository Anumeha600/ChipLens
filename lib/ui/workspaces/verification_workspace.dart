import 'package:flutter/material.dart';

/// Placeholder for the Verification workspace.
///
/// Replace with formal verification UI in a future sprint.
class VerificationWorkspace extends StatelessWidget {
  const VerificationWorkspace({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Verification Workspace'));
  }
}
